# UI/UX 設計規則

本文件定義 FrameFileTool 的介面呈現、使用流程、互動狀態、文案與視覺規則。
工程架構、MVVM 分層、TDD、檔案操作核心行為與新增工具流程，請以
[AGENTS.md](./AGENTS.md) 為準。

## 文件定位

- 本文件是 UI/UX 設計與驗收依據。
- 本文件不定義 planner、executor、service interface 或測試架構。
- 若本文件與 [AGENTS.md](./AGENTS.md) 對工程規則描述不同，以 `AGENTS.md` 為準。
- 若 UI 顯示規則與現有 XAML style 不一致，新增 UI 應優先沿用現有 style，再逐步整理共用樣式。

## 設計目標

- 以工作效率為優先，避免行銷式版面與不必要的視覺裝飾。
- 讓使用者能快速完成「選擇資料夾、掃描、預覽、執行」的固定流程。
- 批次操作必須先讓使用者看懂將發生什麼，再允許執行。
- 錯誤、衝突、略過與取消狀態必須清楚呈現。
- 介面應適合長時間處理大量序列圖檔，資訊密度高但不凌亂。

## 使用者流程

主要流程應維持下列順序：

1. 選擇或輸入資料夾。
2. 設定副檔名與是否包含子資料夾。
3. 掃描檔案。
4. 選擇工具並調整參數。
5. 產生預覽。
6. 檢查預覽摘要、預覽表格與 log。
7. 預覽無錯誤時執行操作。
8. 執行完成後更新 log 並重新掃描。

## 互動狀態規則

- 尚未掃描或掃描結果沒有檔案時，不可預覽。
- 尚未產生預覽時，不可執行。
- 預覽含錯誤時，不可執行。
- 使用者變更會影響計畫的設定後，既有預覽必須失效。
- 掃描結果可保留；預覽結果可因參數變更而清除。
- 切換工具時應清除既有預覽，避免左側工具與右側預覽內容不一致。
- 長時間工作不得凍結 UI，必須有進度或狀態回饋。
- 縮放執行中應停用掃描、預覽與其他執行操作，只保留取消縮放。
- 取消操作後，必須顯示已完成、略過或取消的結果。
- 執行完成後應重新掃描，避免畫面保留已不存在或已改名的檔案。

## 資訊架構

主視窗應維持下列區塊順序：

1. 資料夾與掃描設定。
2. 工具參數設定。
3. 預覽摘要與預覽表格。
4. 可摺疊 log。

工具以全寬 `TabControl` 呈現，每個工具獨立一個 `TabItem`，標籤列橫跨主要內容區頂部。
每個 `TabItem` 內部維持左右分欄：工具參數在左（290px），預覽摘要與預覽表格在右（\*）。
掃描設定列與 Log 區塊位於 `TabControl` 上下，所有分頁共用。
切換分頁時，若既有預覽屬於不同工具，預覽會自動清除（現有邏輯保留）。

工具參數應依工具分組，不同工具的專屬欄位不可混在同一組設定裡。
預覽表格應依目前工具切換對應欄位，避免在共用表格累積無關欄位。

## 版面與排版 Token

### 版面原則

- 介面應偏向密集、清楚、可掃描。
- 主要操作區應保持穩定位置，避免操作後大幅跳動。
- 同一類控制項應對齊，欄位標籤與輸入框距離應一致。
- 不使用大面積 hero、宣傳文案、裝飾插圖或不必要動畫。
- 卡片只用於明確分隔工具區塊、預覽區塊或 log，不做巢狀卡片。
- 長內容區，例如預覽表格，應優先取得剩餘空間。

### 間距與尺寸

| 用途               | 建議值         |
| ------------------ | -------------- |
| 第一層頁面邊距     | 12px 到 16px   |
| 區塊內距           | 10px 到 14px   |
| 密集表單內距       | 8px            |
| 相鄰區塊垂直距離   | 10px 到 12px   |
| 同列控制項水平距離 | 6px 到 8px     |
| 表單標籤與輸入距離 | 不超過 4px     |
| 卡片圓角           | 不超過 8px     |
| Log 展開高度       | 100px 到 140px |

### 視窗縮放

- 主視窗最小寬度應能容納資料夾列、工具參數與主要按鈕。
- 視窗變窄時，表格應優先保留檔名、動作與狀態欄位。
- Log 展開時不應過度擠壓主要預覽區。
- 表格欄寬應穩定，避免資料更新時大幅跳動。

## 字體與字級

### 字體

- 一般 UI 字體使用 `Segoe UI`。
- Log 與路徑密集文字使用 `Cascadia Code, Consolas, monospace`。
- Unicode 圖示可使用 `Segoe UI Emoji`。
- 不混用裝飾字體。

### 字級

| 用途            | 建議字級 | 說明                   |
| --------------- | -------- | ---------------------- |
| 視窗主要標題    | 16px     | 只用於最上層標題       |
| 區塊標題        | 13px     | 搭配 SemiBold          |
| 一般文字        | 12px     | 預設 UI 與表格主要文字 |
| 欄位標籤        | 11px     | 放在輸入框上方或旁邊   |
| Hint 與輔助文字 | 11px     | 使用次要文字色         |
| 表格次要資訊    | 11px     | 動作、狀態補充         |
| Log 文字        | 11px     | 使用固定寬度字體       |
| 按鈕文字        | 12px     | 主要按鈕可 SemiBold    |

- 字級不得依視窗寬度動態縮放。
- 字距維持預設值，不使用負字距。
- `FontWeight` 只用於標題、主要按鈕與重要狀態。
- 同一區塊內最多使用兩種字級，避免資訊層級混亂。

## 色彩與狀態

- 主要動作使用藍色系，代表預覽、掃描等非破壞性操作。
- 危險或破壞性操作使用紅色系。
- 警告使用橙色或琥珀色，不與錯誤狀態混用。
- 工具專屬 accent color 預設不使用；若未來需要，必須先在本文件定義語意與使用範圍。
- 背景與表格使用中性色，避免高飽和色主導整個畫面。
- 停用狀態必須明顯低對比，但仍可讀。
- 顏色不可作為唯一狀態提示，必須搭配文字或圖示。

狀態語意應維持一致：

| 狀態 | 用途                             |
| ---- | -------------------------------- |
| 資訊 | 掃描完成、預覽完成、執行完成     |
| 警告 | 覆寫模式、取消中、需要使用者注意 |
| 錯誤 | 無效輸入、目標衝突、檔案不存在   |
| 危險 | 即將執行會改變檔案的操作         |

## 元件規則

### 按鈕與命令

- 一般按鈕高度建議為 32px 到 34px。
- 小型次要按鈕高度可為 22px 到 28px。
- 按鈕水平內距建議為 10px 到 14px。
- 主要按鈕與危險按鈕應有一致高度與字重。
- 按鈕文字優先使用 2 到 6 個中文字。
- 按鈕應依命令重要性套用一致樣式，不為單一情境建立特殊樣式。
- 正常操作圖示不可使用錯誤語意符號，例如不可用叉號代表正常刪除。
- 裝飾性圖示不可用來代表核心操作，例如不可用閃光符號代表改名。

按鈕層級：

| 層級 | 用途                         |
| ---- | ---------------------------- |
| 主要 | 掃描、預覽                   |
| 危險 | 執行刪除、執行改名、覆寫縮放 |
| 次要 | 選擇資料夾、清除 log         |
| 中止 | 取消縮放                     |

### 輸入控制項

- TextBox、ComboBox 與數值輸入高度建議為 30px 到 32px。
- 短數值欄位寬度建議為 72px 到 96px。
- 檔名、前綴與子資料夾名稱欄位可水平伸展。
- CheckBox 與 toggle 用於二元設定，例如包含子資料夾、保留比例。
- ComboBox 用於有限選項，例如縮放演算法與輸出模式。
- Tooltip 可用於圖示或縮寫，但不可取代必要標籤。
- 數值欄位若輸入無效內容，應在預覽前或預覽時給出明確錯誤，不可靜默套用預設值。

### ProgressBar 與 Busy 狀態

- ProgressBar 只在長時間操作顯示。
- ProgressBar 必須搭配文字進度，例如目前筆數與檔名。
- 掃描中、讀取圖片尺寸中、縮放中都應有明確狀態回饋。
- 進度區塊不應造成主要版面大幅跳動。

## 表格與預覽

每個工具的預覽表格都應包含：

- 序號。
- 原始檔名。
- 操作動作。
- 目標檔名、目標位置或刪除狀態。
- 驗證狀態。

工具專屬欄位只出現在該工具的 DataTemplate：

| 工具     | 必要欄位                       |
| -------- | ------------------------------ |
| 抽幀刪除 | 刪除或保留狀態                 |
| 批次改名 | 目標檔名與衝突狀態             |
| 批次縮放 | 原始尺寸、縮放後尺寸與輸出目標 |

表格呈現規則：

- DataGrid 列高建議為 40px 到 48px。
- 序號欄寬建議為 44px 到 52px。
- 尺寸欄寬建議為 100px 到 120px。
- 表格中的檔名欄應比狀態欄有更高優先寬度。
- 表格不應因 hover、選取或錯誤狀態改變列高。
- 長檔名可使用截斷顯示，但必須能透過 tooltip、log 或詳情定位完整檔名。
- 目標檔名與目標位置也應支援截斷顯示與完整內容檢視。

## Empty State

- 尚未掃描時，摘要應顯示「尚未掃描」或等價文字。
- 掃描後沒有符合檔案時，應顯示掃描數量為 0，並停用預覽。
- 尚未產生預覽時，預覽摘要應提示選擇操作並點擊預覽。
- 預覽結果為空時，不應允許執行。
- Empty state 不放教學長文，只提供下一個可執行動作的短提示。

## Log 與訊息

- Log 預設可摺疊，避免壓縮主要工作區。
- 最新訊息顯示在最上方。
- Log 使用固定寬度字體，方便掃描時間與路徑。
- 掃描錯誤、執行錯誤與取消狀態都應寫入 log。
- Log 訊息應包含操作、結果數量與必要路徑。
- 清除 log 只影響畫面訊息，不影響檔案與預覽狀態。

錯誤訊息格式建議：

```text
對象：原因
```

範例：

```text
frame001.png：目標檔案已存在
```

## 危險操作呈現規則

- 所有會改變檔案的命令都必須由使用者明確觸發。
- 執行按鈕應與預覽按鈕有明顯視覺區隔。
- 覆寫模式必須在 log 或狀態區提示原始檔會被取代。
- 預覽過期後不可繼續執行。
- 核心檔案操作安全規則以 [AGENTS.md](./AGENTS.md) 的「檔案操作安全規則」為準。

## 文字與用詞

- UI 文字以繁體中文為主。
- 技術詞可保留英文，例如 `ViewModel`、`Service`、`preview`、`log`。
- 按鈕文字使用動詞開頭，例如「掃描」、「預覽」、「執行」、「取消」。
- 區塊標題優先使用 2 到 8 個中文字。
- Hint 單句不超過 30 個中文字；需要更長說明時應拆到文件。
- 不在畫面上放置教學式長文。

用詞一致性：

| 用詞 | 定義                           |
| ---- | ------------------------------ |
| 掃描 | 讀取資料夾內檔案清單           |
| 預覽 | 產生尚未執行的操作計畫         |
| 執行 | 實際改變檔案                   |
| 略過 | 項目不符合執行條件或已被取消   |
| 錯誤 | 使用者需要修正設定或檔案狀態   |
| 取消 | 使用者中止尚未完成的長時間操作 |

## 無障礙與可讀性

- 文字不得被按鈕、表格欄位或其他元素截斷到無法判讀。
- 表格列高應足以容納檔名、動作與狀態。
- 停用控制項仍應能看出用途。
- 常見操作不可只靠顏色辨識。
- 鍵盤焦點樣式應可見。
- 文字與背景應維持足夠對比。

## 新增 UI 檢查清單

- 是否符合主要使用者流程。
- 是否有必要的 empty、busy、error 狀態。
- 是否先產生預覽，再允許執行。
- 是否在預覽摘要顯示數量與錯誤狀態。
- 是否將掃描錯誤、執行錯誤與取消狀態寫入 log。
- 是否符合字級、間距、按鈕層級與表格規則。
- 是否能處理長檔名、窄視窗與大量檔案。
- 是否符合本文件的文字、色彩與危險操作呈現規則。
