# TODO

本文件追蹤目前已確認但尚未完成的功能計劃。
執行功能開發前，請先確認本文件是否有相關項目，並在完成後更新狀態。

執行本文件項目時，必須確實同步更新本文件：

- 開始執行某個功能群組時，將該群組正在處理的項目標記為 `[~]`。
- 每完成一個子項目，立即將該項目更新為 `[x]`，不可等到整批功能結束才回填。
- 若實作過程發現範圍、風險或完成判定需要調整，必須在同一輪變更中更新本文件。
- 若某項目暫停、拆分或延後，必須保留未完成狀態，並補上原因或後續處理方向。
- 每次 commit 前，必須確認本文件狀態與實際完成內容一致。

## 狀態標記

- `[ ]` 尚未開始。
- `[~]` 進行中。
- `[x]` 已完成並通過驗證。

## 近期功能計劃

### 1. 拖曳檔案或資料夾新增資料

- [x] 新增拖放匯入服務，將 dropped paths 轉成 `FileItem` 清單與錯誤訊息。
- [x] 支援拖曳單一檔案、多個檔案、資料夾，以及檔案與資料夾混合輸入。
- [x] 拖入資料夾時套用目前的「包含子資料夾」設定。
- [x] 拖入檔案時套用目前勾選的副檔名過濾規則。
- [x] 重複路徑不得重複加入 `Files`。
- [x] 拖放成功後必須清除既有預覽，避免預覽與檔案清單不一致。
- [x] 拖放略過或錯誤項目必須寫入 log。
- [x] 預覽區提供明確 drop target 狀態，不影響既有 empty state。
- [x] 補上拖放匯入服務測試。

完成判定：

- [x] 拖入支援格式檔案後，檔案出現在可預覽資料清單中。
- [x] 拖入資料夾後，依目前副檔名與子資料夾設定匯入。
- [x] 拖入不支援副檔名、重複檔案或無法讀取路徑時，log 有明確訊息。
- [x] 拖放後既有 `CurrentPreview` 失效，執行按鈕不可沿用舊預覽。

### 2. 可勾選要變動的檔案

- [ ] 為預覽項目新增可繫結的 `IsIncluded` 狀態。
- [ ] 預設只勾選可執行項目，例如刪除、改名、縮放。
- [ ] 錯誤項目與不處理項目不可勾選或不會被 executor 執行。
- [ ] 三個預覽 DataTemplate 都加入 checkbox 欄位。
- [ ] 預覽摘要依已勾選項目重新計算。
- [ ] 當沒有任何可執行且已勾選的項目時，執行按鈕必須停用。
- [ ] executor 只執行 `IsIncluded`、無錯誤且動作符合的項目。
- [ ] 補上 PreviewViewModel、CanExecute 與 executor 測試。

完成判定：

- [ ] 使用者可取消勾選單一項目，該項目執行時會被略過。
- [ ] 錯誤列即使顯示在表格中，也不會被執行。
- [ ] 摘要文字與執行按鈕狀態會隨勾選變化同步更新。
- [ ] 抽幀刪除、批次改名、批次縮放三個工具行為一致。

### 3. 抽幀刪除、批次改名、批次縮放均可指定輸出資料夾

#### 抽幀刪除

- [ ] 新增輸出模式選項：「移動到指定資料夾」（保留原始檔，將保留的幀複製到目標資料夾）。
- [ ] UI 在抽幀刪除操作面板加入輸出資料夾選項與選擇按鈕。
- [ ] Planner 檢查指定資料夾路徑是否合法，並偵測目標衝突。
- [ ] Executor 支援移動到指定資料夾的執行路徑。
- [ ] 補上 planner 與 executor 測試。

#### 批次改名

- [ ] 新增輸出模式選項：「複製改名到指定資料夾」（不修改原始目錄）。
- [ ] UI 在批次改名操作面板加入輸出資料夾選項與選擇按鈕。
- [ ] Planner 檢查指定資料夾路徑是否合法，並偵測目標衝突。
- [ ] Executor 支援複製改名到指定資料夾的執行路徑。
- [ ] 補上 planner 與 executor 測試。

#### 批次縮放

- [ ] 擴充 `ResizeOutputMode`，加入指定資料夾輸出模式。
- [ ] 擴充 `ResizeOptions`，加入指定輸出資料夾路徑。
- [ ] UI 在批次縮放輸出位置加入「指定資料夾」選項與選擇資料夾按鈕。
- [ ] 指定資料夾路徑變更時，既有預覽必須失效。
- [ ] Planner 檢查指定資料夾路徑是否合法。
- [ ] Planner 檢查目標檔案是否已存在。
- [ ] 同名檔案輸出到同一指定資料夾時，預覽必須標示衝突。
- [ ] Executor 將縮放結果輸出到指定資料夾。
- [ ] 補上 planner、preview service、executor 與 ViewModel 測試。

完成判定：

- [ ] 三個工具均可選擇指定輸出資料夾並產生對應預覽。
- [ ] 目標資料夾不存在時，執行階段可安全建立資料夾。
- [ ] 目標檔已存在或同名衝突時，預覽顯示錯誤並停用執行。
- [ ] 批次縮放：覆寫原檔、另存子資料夾、指定資料夾三種輸出模式都維持可用。

### 4. 批次縮放改用倍率輸入

- [ ] 將目前百分比縮放輸入改為倍率輸入，例如 `1` 代表原尺寸、`0.5` 代表縮小一半、`2` 代表放大兩倍。
- [ ] 調整 UI 文案，避免同時出現百分比與倍率造成混淆。
- [ ] 調整 `ResizeOptions` 與 planner 驗證規則，倍率必須大於 0。
- [ ] 調整預覽狀態文字與目標尺寸計算，使用倍率計算輸出尺寸。
- [ ] 調整 executor 的 Magick.NET resize geometry 建立方式。
- [ ] 補上倍率小於等於 0、倍率 1、倍率 0.5、倍率 2 的測試。

完成判定：

- [ ] UI 顯示倍率輸入，不再要求使用者輸入百分比。
- [ ] 倍率 `1` 產生與原圖相同的目標尺寸。
- [ ] 倍率 `0.5` 產生約一半尺寸，至少保留 1px。
- [ ] 倍率 `2` 產生兩倍尺寸。
- [ ] 舊的百分比文案、log 與錯誤訊息已同步改為倍率語意。

## 通用驗證

每完成一個功能群組，都必須執行：

```powershell
dotnet test -p:UseAppHost=false
dotnet format --verify-no-changes --severity warn
npx markdownlint-cli2 "*.md"
```

若有修改 UI，也需要手動確認：

- [ ] 空狀態、錯誤狀態、忙碌狀態都能正確顯示。
- [ ] 預覽必須先產生，執行按鈕才可使用。
- [ ] 長檔名與大量檔案不造成表格排版破裂。
- [ ] Log 訊息能定位發生問題的檔案或資料夾。
